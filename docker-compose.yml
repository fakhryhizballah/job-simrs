version: '3.8'

services:
  app:
    build: .
    container_name: jobs-satu-sehat
    # ports:
    #   - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules # Anonymous volume agar node_modules container tidak tertimpa host
    environment:
      - NODE_ENV=development